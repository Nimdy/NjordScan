"""njordscan monitor — a local-first operational security dashboard.

Register multiple projects (folders, git URLs, live URLs), re-scan them on a schedule,
track findings appear/get-fixed/regress over time, and get alerted when a new critical
shows up. Private and local: no account, no cloud, all under ~/.njordscan/monitor.
"""
