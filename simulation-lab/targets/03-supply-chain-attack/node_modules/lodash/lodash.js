'use strict';
// Trimmed stand-in for lodash 4.17.11 (the real package is much larger).
// Pinned to a known-vulnerable version on purpose for the lab.
function get(obj, path, def) {
  const keys = Array.isArray(path) ? path : String(path).split('.');
  let cur = obj;
  for (const k of keys) {
    if (cur == null) return def;
    cur = cur[k];
  }
  return cur === undefined ? def : cur;
}
function defaultsDeep(target) {
  return target;
}
module.exports = { get, defaultsDeep };
